/**
 * anton's own public repository (anton-ztv7). The single source for every anton URL the runtime
 * emits — the PR footer's attribution link, the bd migration runbook — so the copies can't drift
 * into a link that 404s or, worse, silently resolves to GitHub's homepage.
 */
export const ANTON_REPO_URL = "https://github.com/henriblancke/anton";
